{
  "name": "@scaffold-eth/truffle",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^3.4.1",
    "@truffle/hdwallet-provider": "^1.2.4",
    "ethers": "^5.0.32",
    "js-chain-libs": "^0.3.0",
    "truffle-assertions": "^0.9.2",
    "truffle-hdwallet-provider": "^1.0.17",
    "truffle-hdwallet-provider-privkey": "^0.3.0",
    "web3": "1.3.3"
  },
  "scripts": {
    "deploy-tip": "truffle migrate -f 1 --to 1 --network rinkeby",
    "deploy-badge-minter": "truffle migrate -f 2 --to 2 --network rinkeby",
    "test-tip": "truffle test --network rinkeby ./test/testTip.js",
    "test-badge-minter": "truffle test --network rinkeby ./test/testBadgeMinter.js"
  }
}
